console.log(Number.parseInt("30px", 10)); // By using these I can convert my number into Hexa, Octal Also I can remove the text only when the string will be the last -> the second argument is the base form of a Number system

console.log(Number.parseFloat("2.4rem", 10)); // it is the same as parseInt
console.log(Number.isNaN(90)); // check a number
console.log(Number.isFinite(20)); // better way to check if it us a number

/* Numeric separators */
const trans = 12_300;
